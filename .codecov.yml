coverage:
  status:
    patch:
      default:
        target: '80'
    project:
      library:
        target: auto
        paths:
          - "src/siphon/.*"

      tests:
        target: 100%
        paths:
          - "src/siphon/testing.py"
          - "tests/.*"

  notify:
    gitter:
      default:
        url: "https://webhooks.gitter.im/e/0eb95be6d20af5b9a4f0"

ignore:
  - "**/*_pb2.py"

comment: off
